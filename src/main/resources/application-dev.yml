spring:
  datasource:
    url: jdbc:h2:mem:hdfclife;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true

  flyway:
    enabled: true

hdfc:
  company-name: HDFC Life
  max-claim-amount: 500000

